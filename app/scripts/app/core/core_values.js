'use strict';

// values
mainApp.value('CoreValues', (function () {
    var value = {
        appName: 'fiyuh'
    };

    return  value;
})());


//constants
mainApp.constant('UrlConstants', (function () {
    var constant = {
        home: '/',
        games: '/games'
    };

    return constant;
})());